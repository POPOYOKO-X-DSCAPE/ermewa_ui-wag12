import { css } from "@styles";

const badge = css({
	height: "s.md",
	width: "s.md",
	borderRadius: "100%",
	backgroundColor: "c.badge.bg.initial",
	color: "c.badge.fg.initial",
	fontSize: "s.badge",
});

export const Styles = {
	badge,
};
